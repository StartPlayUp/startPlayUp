import React from 'react';
import {Route} from 'react-router-dom';

const Main = () =>{
    return(
        <div>
            <h2>로그인 화면이 될 예정입니다. react router 연습용.</h2>
        </div>
    );
};

export default Main;